# How to run this example:

## Using Docker Compose
Run the docker compose
```
docker compose up -d
```

## Using Kubernetes 
Create a kubernetes cluster locally using ```minikube```
```
minikube start
```

List all kubernetes pods
```
kubectl get pods -A
```

